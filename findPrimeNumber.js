function solution(numbers) {
    var answer = 0;
    const numArr = numbers.split("");
    const allNum = numArr.map( (num, i) => {
        console.log(num)
    })
    // console.log(numArr)
    return answer;
}

solution("011")